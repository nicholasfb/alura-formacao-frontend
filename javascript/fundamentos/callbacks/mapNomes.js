let nomes = ['ana Julia', 'Caio vinicius', 'BIA silva'];

const nomesCaixaAlta = nomes.map(nome => nome.toUpperCase());
const nomesCaixaBaixa = nomes.map(nome => nome.toLowerCase());

console.log(nomesCaixaAlta);
console.log(nomesCaixaBaixa);